spring:
  application.name: order-processing
  profiles.active: test
spring.security.user:
  name: admin
  password: s3C4E7
management:
  endpoints.web:
    base-path: /management
    exposure.include: health,info,metrics,mappings
  endpoint.health.show-details: WHEN_AUTHORIZED
---
spring:
  profiles: test
  datasource:
    url: jdbc:h2:mem:orders_db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa